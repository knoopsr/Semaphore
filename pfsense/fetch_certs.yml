---
- name: get certs
  fetch:
    src: "/conf/acme/{{ domain_name  }}"
    dest: "/home/semaphore/certs/{{ item }}/"
    flat: yes
  with_items:
    - "{{ item }}.fullchain" 
    - "{{ item }}.ca"
    - "{{ item }}.key"
    - "{{ item }}.crt"