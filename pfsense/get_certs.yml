---
- hosts: all
  gather_facts: false
  become: true
  become_user: root

  vars:
    ansible_python_interpreter: /usr/local/bin/python3.11
    domain_names: "{{ lookup('file', '/home/semaphore/vars/cert_domain_names.yml') | from_yaml }}"

  tasks:
    - name: Loop fetch_certs.yml
      include_tasks: fetch_certs.yml
      loop: "{{ domain_names }}"
